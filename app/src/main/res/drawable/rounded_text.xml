<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo -->
    <solid android:color="#FFFFFF"/>

    <!-- Bordes -->
    <stroke
        android:width="2dp"
        android:color="@color/hintColor"/> <!-- Color del borde -->

    <!-- Bordes redondeados -->
    <corners android:radius="8dp"/> <!-- Radio de los bordes redondeados -->

    <!-- Padding interno (opcional) -->
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp"/>
</shape>